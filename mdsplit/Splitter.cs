using System;
namespace mdsplit {
    public class Splitter {
        // Splitter is going to have a list (array list?) to store the results of the split.
        public Splitter() {
        }
    }
}
